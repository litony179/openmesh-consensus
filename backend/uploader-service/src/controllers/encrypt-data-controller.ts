import { Request, Response } from "express";
import asyncHandler from "express-async-handler";

const encryptdataHandler = asyncHandler(async (req: Request, res: Response) => {
    
});

export { encryptdataHandler };