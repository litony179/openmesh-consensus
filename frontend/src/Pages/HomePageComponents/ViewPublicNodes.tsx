import React, { useState } from 'react';

/**
 * Functions Required:
 * 1. fetch public node based on request.
 * 2. Get node raw data 
 * 
 * A page where you can view
 * 1. All public nodes (based on data type and connection type)
 * 2. All public nodes (based on connection type)
 * 
 * REQUIREMENT: ability to inspect node.
 * 
 * @returns 
 */
export const ViewPublicNodes = () => {
  // State for storing selection values

  return (
    <>
      <div>
        view all public nodes  and its data
      </div>
    </>
  );
};